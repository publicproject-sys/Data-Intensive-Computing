#Build docker image from Dockerfile
docker build -t dic-assignment .

# Run docker container locally
docker run -d -p 5000:5000 dic-assignment

Vladimir progress:
Two files app.py and request.py,! first start app and the execute request;!
Ignore PART 1_TO Ignore
Input is a series of files
Output is a array with subarrays with the structure (filename,classes)

Also uploaded the small files, and a folder with just two files


